<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
             <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#section1">HelpingHANDS</a>
        </div>
        <div class="navbar-collapse collapse" id="navbar">
            <ul class="nav navbar-nav navbar-left">
                <li><a href="#section2">What</a></li>
                <li class="divider"></li>
                <li><a href="#section3">xxx</a></li>
                <li class="divider"></li>
                <li><a href="#section4">How</a></li>
                <li class="divider"></li>
                <li><a href="#section5">Why</a></li>
            </ul>
            <ul class="nav navbar-right navbar-nav">
                <li>
                    <% if current_user%>
                        <% if current_user_type=="donor"%>
                            <a class="dropdown-toggle" id="menu1" data-toggle="dropdown">
                                Welcome &nbsp;
                                <% if current_user.nil?%>
                                    <%=current_user.fname%>!
                                <%else%>
                                    Donor!
                                <%end%>
                                <span class="caret"></span>
                            </a>
                        <% elsif current_user_type=="charity"%>
                            <a class="dropdown-toggle" id="menu1" data-toggle="dropdown">
                                Welcome &nbsp;
                                <% if current_user.nil?%>
                                    <%= current_user.charityName %>
                                <%else%>
                                    Charity!
                                <%end%>
                                <span class="caret"></span>
                            </a>
                        <% elsif current_user_type=="admin"%>
                            <a class="dropdown-toggle" id="menu1" data-toggle="dropdown">
                                Welcome &nbsp;
                                <% if current_user.nil?%>
                                    <%=current_user.fname%>!
                                <%else%>
                                    Admin!
                                <%end%>
                                <span class="caret"></span>
                            </a>
                        <%end%>
                        <ul class="dropdown-menu" role="menu" aria-labelledby="menu1">
                            <li role="presentation" class=""><%=link_to :Settings,settings_path %></li>
                            <li role="presentation" class=""><%= link_to :Logout, logout_path %></li>
                        </u>l
                    <% else %>
                        <a href="#" class="btn" data-toggle="modal" data-target="#loginModal">
                            LOGIN/SIGNUP
                        </a>
                    <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>
        
<%=render 'layouts/login_modal'%>